module.exports = {
  parser: "babel-eslint",
  extends: "optimum-energy",
  env: {
    <%= browser ? 'browser' : 'node' %>: true,
    jest: true
  },
  parserOptions: {
    sourceType: "module"
  }
}
